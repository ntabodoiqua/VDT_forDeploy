server:
  port: 8080
  servlet:
    context-path: /lms

spring:
  datasource:
    url: "jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:identity}"
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
  servlet:
    multipart:
      max-file-size: 250MB
      max-request-size: 250MB
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

jwt:
  signerKey: "${JWT_SIGNER_KEY:U+NXeAi5ytRMjd8P5J7H6Upqm3Hvt7dyliIW5rosLvAGb3wiS6HBLj1UmbQgD485}"
  valid-duration: 3600 #in second
  refreshable-duration: 36000 #in second

recaptcha:
  secret: ${RECAPTCHA_SECRET:6LcNlVArAAAAAABVgugTwNHe5zM34fIJmYC_lm8w}

digitalocean:
  spaces:
    access-key: ${DO_SPACES_ACCESS_KEY:DO00XXLFWXMLW74Z2XTP}
    secret-key: ${DO_SPACES_SECRET_KEY:db+m2lDcfWzZHBVD9GEDQcej97aAGOgo79fopCkFw3g}
    endpoint-url: https://sgp1.digitaloceanspaces.com # Endpoint của region bạn dùng
    region: sgp1                             # Tên region
    bucket-name: innolearn-files             # Tên bucket (Space) của bạn
    base-url: https://innolearn-files.sgp1.digitaloceanspaces.com # URL công khai của Space